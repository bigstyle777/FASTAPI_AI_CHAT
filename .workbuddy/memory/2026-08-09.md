# 2026-08-09 工作日志

## Vue 前端重构
将 `frontend-old`（原生 JS）的完整功能用 Vue 3 + TypeScript + Vite 重写，代码位于 `frontend/` 目录。

### 完成的功能
- 登录/注册页面（含验证码）
- 聊天主界面（会话列表、流式消息、Markdown 渲染、代码高亮、Mermaid 流程图、KaTeX 数学公式）
- 会话管理（新建、删除、重命名、置顶、分支）
- 消息操作（修改、删除、分支、点赞/点踩、复制）
- Token 使用统计展示
- 个人中心（用户信息、API Key 设置）
- 管理员面板（用户/角色/权限管理）
- 主题切换（浅色/深色/系统）

### 技术架构
- **框架**: Vue 3 + TypeScript + Vite 8
- **状态管理**: Pinia（auth、chat、admin、theme 四个 store）
- **路由**: Vue Router（login/chat/profile/admin，带鉴权守卫）
- **API**: 基于 fetch 的封装，支持 JWT 认证和 SSE 流式处理
- **Composables**: useMarkdown（Markdown 渲染+流式渲染）、useContextMenu（右键菜单）
- **依赖**: markdown-it, highlight.js, katex, mermaid, @traptitech/markdown-it-katex
- **Vite 代理**: 配置了 /users, /chat, /admin 路径代理到后端 8001 端口

### 关键文件
- `src/api/` - API 客户端层（client.ts, auth.ts, chat.ts, admin.ts）
- `src/stores/` - Pinia stores（auth.ts, chat.ts, admin.ts, theme.ts）
- `src/composables/` - useMarkdown.ts, useContextMenu.ts
- `src/views/` - LoginView, ChatView, ProfileView, AdminView
- `src/components/chat/` - SessionItem, MessageItem, TokenUsage, RenameModal
- `src/assets/main.css` - 全局样式（从 frontend-old/style.css 迁移）
- `src/types/index.ts` - TypeScript 类型定义

### 构建状态
- TypeScript 类型检查通过
- Vite 构建成功
- 开发服务器运行在 http://localhost:5173

## Bug 修复（18:10）
### 用户消息不显示
- **根因**: MessageItem.renderContent() 只处理 assistant 消息，user 消息的 DOM（contentRef）从未被写入
- **修复**: 增加 user 角色分支，直接设置 textContent

### 消息发送后卡顿
- **根因**: sendMessage/modifyMessage 完成后调用 loadSessions() → loadSessionMessages() 替换整个 messages 数组，导致所有 MessageItem 销毁重建
- **修复**: 新增 refreshSessionsOnly() 方法只更新会话列表侧边栏；sendMessage、modifyMessage、togglePin 改为调用此方法

### Markdown 渲染颜色太淡
- **根因**: 未导入 highlight.js 和 KaTeX CSS 主题；markdown-body 缺少元素级样式（标题、引用、表格、行内代码等）
- **修复**: 
  - main.ts 导入 highlight.js/styles/github.css 和 katex/dist/katex.min.css
  - main.css 补充 h1-h6/p/ul/ol/blockquote/code/pre/table/th/td/a/hr/strong/img 完整样式
  - 深色主题下对应属性添加 [data-theme="dark"] 覆盖

## 思考中动画（18:36）
- **需求**: AI 回复生成前显示「正在思考中……」带动画效果
- **实现**:
  - MessageItem.vue 新增 isThinking() 判断（isStreaming + role===assistant + content为空）
  - 模板中 thinking-indicator 带 CSS 脉冲动画的三个点（bounce 动画，间隔 0.2s）
  - 思考状态下 renderContent 跳过，模板中的指示器接管显示
  - 内容到达后 message.content 非空 → isThinking() 返回 false → 切换到正常渲染

## SPA 路由代理冲突修复（18:47）
- **问题**: 刷新 /chat、/admin 页面时 Vite 代理将前端路由转发到后端，返回 `{"detail":"Not Found"}`
- **根因**: Vite proxy 是前缀匹配，`/chat` 规则将所有 `/chat*` 请求代理到后端，Vue Router 无法处理
- **修复**: 自定义 spaFallbackPlugin 插件，通过 configureServer 注入中间件：
  - 带子路径（`/users/`、`/chat/`、`/admin/`）→ API 请求，放行给代理
  - 无扩展名的 GET 请求 → SPA 路由，改写为 `/index.html`
- **第二次修复（18:51）**: 改用 `configureServer` 自定义中间件改写 SPA 路由 → 但条件太宽（`!url.includes('.')`）把 `/@vite/client` 等 Vite 内部路径也改写了，导致 dev server 彻底挂掉
- **第三次修复（18:56）**: 改为精确匹配已知 SPA 路由集合 `['/', '/chat', '/login', '/profile', '/admin']`，不再影响 Vite 内部路径和 API 代理
- **验证**: 所有 SPA 路由(200)、API 代理(401/405)、Vite 内部路径(200) 全部正常
