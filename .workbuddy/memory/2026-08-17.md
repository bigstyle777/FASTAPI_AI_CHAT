# 2026-08-17

## 前端：模式下拉 + 记忆管理模块
- ChatView 左上角品牌改为下拉框（AIChatPro / Work），存 uiStore.chatMode（localStorage 持久化）；Work 模式发送走 /agent/stream（chatStore.sendAgentMessage），AIChatPro 走原 /chat/stream。
- 新增 /memory 路由 + MemoryView.vue：记忆的增删改查，对接后端 /memories（注意：后端返回 memory_id，api/memory.ts 归一化为 id）。
- ChatView、KnowledgeView 侧边栏“知识库”下方加“记忆”入口。
- 样式全部走项目自定义 CSS 设计系统（main.css CSS 变量，自动适配暗色主题）；项目未使用 tailwind/shadcn-vue/axios，API 复用现有 fetch 封装 api/client.ts（自带 token 与 401 处理）。
- npm run type-check 通过。

## 记忆页模式下拉（第二轮调整）
- ui store 统一全局状态 currentMode: 'AIChatPro' | 'Work'（localStorage 键 chat-mode，兼容旧小写值）；ChatView 下拉同步改用 currentMode。
- MemoryView 左上角品牌文字换成同款小下拉（复用 .brand-mode-select，无新增CSS）；watch currentMode 变化即退出编辑/删除态并重新拉取列表。
- api/memory.ts 四个方法加 mode 参数：AIChatPro → /memories，Work → /agent/memories。
- 注意：后端 agent 目录目前只有 /agent/stream、/agent/runs，/agent/memories 尚不存在，Work 模式记忆接口待后端补齐。
- type-check 通过。

## 回退第二轮改动
- 应用户要求撤销第二轮：MemoryView 恢复无下拉、固定调 /memories；api/memory.ts 去掉 mode 参数；ui store 回到 chatMode('aichatpro'|'work')；ChatView 下拉恢复小写取值。
- 保留第一轮的：MemoryView 记忆页、/memory 路由、侧边栏“记忆”入口、ChatView 品牌下拉（chatMode 版）、main.css 样式。
- type-check 通过。
